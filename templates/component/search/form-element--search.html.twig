{# @orig file templates/base/form-element.html.twig #}
{%
  set classes = [
    'js-form-item',
    'js-form-type-' ~ type|clean_class,
    'js-form-item-' ~ name|clean_class,
    disabled == 'disabled' ? 'form-disabled',
    errors ? 'form-item--error',
  ]
%}
{%
  set remove_classes = [


  ]
%}

{%
  set description_classes = [
    'description',
    description_display == 'invisible' ? 'visually-hidden',
  ]
%}
<!-- <div{{ attributes.addClass(classes).removeClass(remove_classes).removeAttribute('id') }}> -->
  {# label before/inviisble #}
  {% if label_display in ['before', 'invisible'] %}
    {{ label }}
  {% endif %}

  {# prefix #}
  {% if prefix is not empty %}
    <span class="field-prefix">{{ prefix }}</span>
  {% endif %}

  {# description before #}
  {% if description_display == 'before' and description.content %}
    <div{{ description.attributes }}>
      {{ description.content }}
    </div>
  {% endif %}

  {# children =the content of a field. #}
  {{ children }}

  {# suffix #}
  {% if suffix is not empty %}
    <span class="field-suffix">{{ suffix }}</span>
  {% endif %}

  {# label after #}
  {% if label_display == 'after' %}
    {{ label }}
  {% endif %}

  {# errors #}
  {% if errors %}
    <div class="form-error">
      {{ errors }}
    </div>
  {% endif %}

  {# description after/invisible #}
  {% if description_display in ['after', 'invisible'] and description.content %}
    <div{{ description.attributes.addClass(description_classes) }}>
      {{ description.content }}
    </div>
  {% endif %}

<!-- </div> -->
