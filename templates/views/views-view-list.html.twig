{#
/**
 * @file
 * Theme override for a view template to display a list of rows.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows for this list.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 * - list: @todo.
 *   - type: Starting tag will be either a ul or ol.
 *   - attributes: HTML attributes for the list element.
 *
 * @see template_preprocess_views_view_list()
 */
#}

{# we dont really need this but for funzies lets keep it #}
{% if attributes !="" -%}
  <div{{ attributes }}>
{% endif %}

  {% if title %}
    <h3>{{ title }}</h3>
  {% endif %}

  {#
    oki then why a user wants to change this is - this is theming
    not a config thing
    @TODO:  list.attributes.addClass('tiles') dont seems
  #}
  <{{ list.type }} class="layout-pagewidth layout">

    {# for loop #}
    {% for row in rows %}
      {# first #}
      {% if loop.first %}
        <li{{ row.attributes.addClass('width-half', 'width-full@s') }}>{{ row.content }}</li>
      {% elseif loop.index <= "3" %}
      {# under 3 #}
        <li{{ row.attributes.addClass('width-fourth', 'width-half@s') }}>{{ row.content }}</li>
      {% elseif (loop.index >= "4") and (loop.index <= "5") %}
      {# 4 + 5 #}
        <li{{ row.attributes.addClass('width-fourth') }}>{{ row.content }}</li>
      {% elseif loop.index == "6" %}
      {# 6 #}
        <li{{ row.attributes.addClass('width-half') }}>{{ row.content }}</li>
      {% else %}
      {# default #}
        <li{{ row.attributes.addClass('width-fourth') }}>{{ row.content }}</li>
      {% endif %}
    {% endfor %}


  </{{ list.type }}>

{# we dont really need this but for funzies lets keep it #}
{% if attributes !="" -%}
  </div>
{% endif %}
